<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css" />
    <title>{{pageTitle}}</title>

    {{#if formsCSS}}
      <link rel="stylesheet" href="/css/forms.css" />
    {{/if}}
    {{#if productCSS}}
      <link rel="stylesheet" href="/css/product.css" />
    {{/if}}

  </head>

  <body>

    <header class="header">
      <nav class="header__nav">
        <ul class="header__item-list">
          <li class="header__item"><a class="{{#if activeShop }} active{{/if}}" href="/">Shop</a></li>
          <li class="header__item"><a class="{{#if activeAddProduct }} active{{/if}}" href="/admin/add-product">Add Product</a></li>
        </ul>
      </nav>
    </header>
    {{{body}}}
  </body>

</html>